import { ReturnButtonStyle } from "./ReturnButton.styled";

const ReturnButtonProfile = ({ onClick }) => {
  return <ReturnButtonStyle onClick={onClick}>Return</ReturnButtonStyle>;
};

export default ReturnButtonProfile;
